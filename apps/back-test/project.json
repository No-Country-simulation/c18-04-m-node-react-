{
  "name": "back-test",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/back-test/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "back-test:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "back-test:build:development"
        },
        "production": {
          "buildTarget": "back-test:build:production"
        }
      }
    }
  }
}
